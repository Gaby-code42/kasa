import Data from "../../Data"
import PropTypes from 'prop-types'
import { useState } from 'react'

function Card ({title, picture, hostName, hostPicture, rating, location, tag}){
    return(
        <div>

        </div>
    )

}

export default Card